继续 Brief 57，接入阿里云 BSS 只读余额 provider，不实现 Brief 63。

目标：

* 调用阿里云 BssOpenApi `QueryAccountBalance`；
* 使用专用 RAM 用户的 AccessKey；
* 仅允许余额查询，不允许充值、购买、订单创建或资源操作；
* 从本地 secrets 配置读取凭据，不进入日志、Git、管理面响应；
* 解析 `Data.AvailableCashAmount`、`Data.AvailableAmount` 和 `Data.Currency`；
* 低于配置阈值时按现有 spend_monitor contract 写入 proposed；
* 正常余额、API 失败、响应无效、重复检查均增加测试；
* 管理面 `/spend/ledger` 可见；
* 不实现 Brief 63 mandates、自动充值或支付执行。

请先给出：

1. 需要我在 config.local.yaml / secrets.local.yaml 填写的准确字段；
2. 最小 RAM 自定义权限策略；
3. 如何手动触发一次只读检查，确认观察期 Day 1 已开始。

禁止要求我提供主账号 AccessKey，也不要输出或记录完整 AccessKey Secret。
